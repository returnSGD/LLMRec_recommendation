torch>=2.0.0
transformers>=4.30.0
sentencepiece>=0.1.99
numpy>=1.24.0
tqdm>=4.65.0
nltk>=3.8.0
rouge-score>=0.1.2
tensorboard>=2.13.0

# ── Optional: for HuggingFace mirror download (mainland China) ──
# modelscope  # alternative model download source
# huggingface_hub>=0.20.0

# ── Install with mirror ──
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
