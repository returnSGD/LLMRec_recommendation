# Idea: 强化学习驱动的长时序记忆推荐系统 —— 超越 P5 语言模型的动态决策框架

## 1. 背景与动机

推荐系统的核心目标是在信息过载的环境中实现用户与物品的高效匹配。长期以来，主流的推荐方法（协同过滤、矩阵分解、深度序列模型等）主要解决**交互数据的稀疏性**问题。然而，两个更深层次的难题始终未被充分解决：

- **长时序记忆难题**：用户的兴趣具有长期依赖性（例如购买周期、季节性偏好、跨会话的隐性规律），传统模型受限于固定上下文窗口或序列长度，难以捕捉数月甚至数年前的弱信号。
- **动态因果性/反馈循环难题**：推荐系统并不是一个静态的分类器，它的每一次推荐都会改变用户的未来行为，形成**选择性暴露**和**回声室效应**。模型误将"系统展示"当作"用户偏好"，导致训练数据偏差，最终损害长期用户价值。

P5（Recommendation as Language Processing）将推荐统一为文本生成任务，利用预训练语言模型的语义知识缓解了稀疏性问题，并在多任务、零样本迁移上表现优异。**然而，P5本质上仍是一个静态的条件语言模型**：
- 没有真正的**长期记忆机制**（仅依赖输入文本中的历史截断，上限 512 token）；
- 没有**动态决策与策略优化**（不建模推荐动作对未来状态的因果影响）；
- 依赖单一的监督学习损失，无法优化用户长期留存、多样性等延迟奖励；
- **仅利用正反馈信号**（点击/购买），不区分负反馈（跳过的、退货的、差评的）与中性信号；
- **无法量化推荐置信度**（每次推荐同等置信，不知何时该"保守"何时该"探索"）；
- **Beam search 推理昂贵**（B=20 意味着每次推荐需要 20 次 decoder 前向传播）；
- **无法做多步规划**（只预测"下一个物品"，无"一周后推荐什么"的长期规划能力）。

为此，我们提出**RL + 长时序记忆混合架构**，将强化学习（RL）与外部持久化记忆系统结合，构建一个能够**记住历史、量化不确定性、主动探索、规划未来**的推荐智能体。

---

## 2. 贡献结构（2 核心 + 2 辅助）

### 核心贡献一：外置层次化长时序记忆 + POMDP 决策框架

- 设计**两级记忆结构**：短期记忆（当前会话/最近 N 次交互） + 长期记忆（压缩后的用户兴趣向量、关键事件时间戳、周期性行为模式）。
- 使用**向量数据库**（ChromaDB / FAISS）存储长期记忆，通过向量相似度检索与当前状态最相关的 top-k 记忆片段。
- 记忆更新采用**增量聚合 + 时间衰减**机制，避免灾难性遗忘。时间衰减权重显式编码"距上次事件的时间间隔"，使模型能学习周期性规律（如每 3 个月购买猫粮）。
- 将推荐建模为**部分可观测马尔可夫决策过程（POMDP）**：状态 = 用户兴趣嵌入 + 长期记忆检索结果 + 记忆不确定性；动作 = 宏观推荐策略（如 `exploit_high_ctr`、`explore_new_category`、`remind_periodic_purchase`、`increase_diversity`）；奖励 = 加权组合（点击 + 停留时长 + 长期留存信号 + 探索奖励 - 负反馈惩罚）。
- **与 MR.Rec (2025.10) 的关键差异**：(1) 记忆采用向量压缩+时间衰减而非纯文本 RAG；(2) POMDP 建模而非普通 MDP；(3) 两级层次化记忆而非单一 RAG 记忆。

### 核心贡献二：RL 主动探索打破回声室

- RL 策略以"长期用户价值"为优化目标（包含多样性指数、会话长度、留存天数），而非仅优化短期 CTR。
- RL 内置探索策略（ε-greedy + 熵正则化），可主动选择 `explore_new_category` 动作，即使短期点击率下降。
- 通过离线 RL（CQL / IQL）结合反事实评估，学习区分"用户真正喜欢的"和"因为频繁推荐才点击的"。
- **与 MocDT (CIKM 2024) 的关键差异**：MocDT 是 Decision Transformer 条件生成（"允许"多样性但不"主动探索"），我们是 RL 主动探索策略；MocDT 无外部记忆。
- 核心实验预期：展示**短期 CTR 略降但长期留存/多样性反超**的反直觉曲线——这是审稿人最喜欢的 insight。

### 辅助贡献三：记忆不确定性 → 自适应探索强度

- 记忆检索模块在输出 top-k 记忆片段的同时输出**检索置信度**（基于向量相似度分布的熵）。
- 检索置信度低（如数据稀疏用户）→ RL 自动增大探索概率，减少对不可靠记忆的依赖。
- 检索置信度高（如规律性购买老用户）→ RL 倾向 exploitation，充分利用可靠记忆。
- 该机制将**记忆系统与 RL 探索策略**自然耦合在一起，形成端到端的自适应系统。
- 实验可做消融：固定 ε vs 记忆不确定性驱动的动态 ε 对比。

### 辅助贡献四：推理效率优势

- P5 做序列推荐需 beam search (B=20)，即 20 次 decoder 前向传播 + log-prob 排序。
- 我们的 RL 策略输出离散动作（<20 种），**1 次前向即可决策**。底层 LLM 渲染层仅在需要生成自然语言解释时才调用，且可用贪心解码。
- 实验中专设一节对比推理延迟与 throughput。

| | P5 (Beam=20) | 本系统 |
|---|---|---|
| 每次推荐的 decoder 调用 | 20 次 | 1 次 (RL 策略) + 0~1 次 (LLM 渲染，可选) |
| 候选排序 | 需要 (基于 log-prob) | 不需要 (动作空间预定义) |
| 记忆检索 | 无 | 1 次向量检索 (<10ms) |

---

## 3. 架构设计

```
flowchart TD
    A[用户交互流] --> B

    subgraph B [记忆系统（向量数据库 ChromaDB / FAISS）]
        B1[短期记忆<br/>（最近 N 次）]
        B2[长期记忆<br/>（压缩向量+时间戳）]
        B3[top-k 检索 + 置信度估计]
        B1 --> B3
        B2 --> B3
    end

    B --> C

    subgraph C [RL 决策层（POMDP 策略网络）]
        C1[输入: 用户嵌入 + 记忆片段 + 置信度]
        C2[输出: 宏观意图（离散动作 <20）+ 探索强度]
        C3[动作掩码: 过滤已购/过敏品类<br/>（确定性规则，非行为树）]
        C1 --> C2 --> C3
    end

    C --> D

    subgraph D [执行 + 渲染层（可选）]
        D1[动作翻译为 API 调用 / 候选集筛选]
        D2[P5 / LLM 渲染自然语言推荐语<br/>（仅在需要解释时调用）]
        D1 --> D2
    end
```

> 注：原 idea 中的"行为树安全层"已移除。确定性安全检查（过滤已购、过敏品类、年龄限制）直接作为 RL action masking 实现，无需引入行为树框架——后者适用于游戏 NPC 的 tick-based 实时决策，用于推荐系统的 if-else 规则是 over-engineering。

---

## 4. 与 P5 Baseline 的对比分析

### 4.1 场景一：长时序记忆依赖 —— 周期性购买预测

**任务定义**：用户每隔固定周期购买某类消耗品（如猫粮、面膜、打印机墨盒），模型需要根据历史购买间隔推荐合适时机。

| 维度 | P5 | 本系统 |
|------|----|--------|
| **信息可用性** | 将过去 K 次交互文本化，若周期超过 512 token 窗口则信息丢失。 | 外部记忆库存储每次购买的时间戳、间隔，可精确检索上一次购买日期和平均周期。 |
| **决策方式** | 条件语言模型直接生成"下一个物品"，无显式时序推理。 | RL 策略根据记忆检索到的周期信息 + 置信度，决策动作 `remind_periodic_purchase`。 |
| **长期依赖建模** | 依赖注意力机制，受限于输入长度。 | 显式存储周期向量 + 时间衰减权重，模型可学习"距上次购买超过 T 天 → 推荐概率上升"。 |
| **不确定性** | 对所有推荐同等置信。 | 记忆置信度低时自动增加探索，避免错误推断周期。 |

### 4.2 场景二：动态因果性 / 反馈循环 —— 多样性促进与回声室破除

**任务定义**：用户长期只点击某类视频（如搞笑短片），模型若持续推荐同类，短期 CTR 高但用户逐渐疲劳并流失。理想模型应适时推荐新颖内容。

| 维度 | P5 | 本系统 |
|------|----|--------|
| **目标优化** | 最大化训练数据中的似然，本质是拟合历史分布。 | 优化长期累积奖励，显式包含多样性指数、会话时长等远期指标。 |
| **探索能力** | 无主动探索；概率采样提供有限随机性。 | RL 内置探索策略，可主动选择 `explore_new_category`。探索强度由记忆不确定性自适应调节。 |
| **因果辨识** | 混淆展示偏差与真实偏好。 | 离线 RL 中的反事实评估 + 负反馈信号建模，可部分去偏。 |
| **负反馈利用** | 不区分正负反馈。 | 显式建模负反馈（快速划走、退货、差评），加入 reward 惩罚项。 |
| **适应性** | 静态数据集训练，环境变化后需重新训练。 | RL 可持续与环境交互，动态适应兴趣迁移。 |

---

## 5. P5 对比实验的控制条件

设计与 P5 论文的对比实验时，必须控制以下条件相同，以保证公平性：

### 5.1 必须相同（Fair Comparison 的底线）

| 控制项 | 具体内容 | 原因 |
|--------|----------|------|
| **数据集** | Amazon (Sports, Beauty, Toys) + Yelp，与 P5 完全相同 | 不同数据集的结果不可比较 |
| **训练/验证/测试划分** | 评分/解释/评论任务：80/10/10 随机划分（保证每用户/物品在训练集至少一次）；序列推荐：留最后一件物品做测试、倒数第二件做验证；直接推荐：遵循序列推荐的训练划分 | P5 的划分策略影响性能，必须一致 |
| **评估指标** | 评分：RMSE, MAE；序列推荐 & 直接推荐：HR@k, NDCG@k；解释 & 评论：BLEU-4, ROUGE-1/2/L；零样本：同上 | 不同指标不可直接对比 |
| **物品池** | 全物品设置（非采样评估），与 P5 一致 | 采样评估会虚高指标 |
| **评估协议** | 序列推荐：Beam search (B=20) 生成候选列表，在全物品空间评估；直接推荐：从候选集中选最优 | P5 的核心推理协议 |
| **任务定义** | 评分（1-5 分预测 & 喜欢/不喜欢）、序列推荐（下一物品预测）、解释生成、评论摘要、直接推荐（top-k），覆盖 P5 全部 5 个任务族 | 不同任务定义无法比较 |

### 5.2 应该报告但可以有差异（Trade-off 讨论）

| 控制项 | P5 的取值 | 我们的处理 | 说明 |
|--------|----------|------------|------|
| **模型总参数量** | P5-S: 60.75M; P5-B: 223.28M | 应控制 RL 策略 + 记忆编码器的额外参数，使总参数量与 P5 可比；或在消融实验中分析参数量对性能的影响 | 我们的系统多了记忆模块和 RL 策略网络，需诚实报告并讨论"性能提升是否单纯来自更多参数" |
| **T5 主干** | T5-small / T5-base (SentencePiece, vocab 32,128) | LLM 渲染层使用相同 T5 检查点（HuggingFace `t5-small` / `t5-base`）初始化 | 保证语言理解能力的起点一致 |
| **最大输入长度** | 512 tokens | 记忆系统的长期信息不受 512 token 限制（这是我们的优势），但应同时报告 P5 在 512/1024/2048 窗口下的性能作为 sensitivity analysis | 如果 P5 在更大窗口下也能追上，则我们的记忆系统优势减弱——需要做这组对照 |
| **训练 epoch / 迭代数** | 10 epochs | 应报告训练计算量（GPU-hours），使读者能判断效率 trade-off | 公平性考量 |
| **预训练数据** | P5 使用 T5 原始预训练权重 | 我们使用相同的 T5 checkpoint | 确保语言知识基线相同 |

### 5.3 可以有差异（这是我们的核心卖点）

| 差异项 | P5 | 本系统 | 说明 |
|--------|-----|--------|------|
| **可用历史长度** | 512 token 窗口截断 | 外部记忆库存储全量历史 | **这正是我们要验证的优势**——应该在实验中设置 P5 在不同窗口下的对照，展示窗口增大时 P5 的边际收益递减，而我们的记忆系统持续受益 |
| **训练目标** | 语言建模负对数似然 | RL 累积奖励最大化 | 不同的优化范式，在长期指标上应有差异 |
| **推理方式** | Beam search (B=20) | 离散动作选择 (1 次前向) | 推理效率是我们的辅助贡献，实验中专门对比延迟 |
| **负反馈建模** | 无 | 显式建模 | 可以单独做消融：加/不加负反馈信号对 reward 质量的影响 |

### 5.4 实验设计建议

**实验一：复现 P5 Baseline**
- 用 P5 开源代码 + 官方 checkpoint，在 Amazon Beauty/Toys/Sports + Yelp 上跑出全部 5 个任务的结果
- 验证复现结果与论文报告值一致（允许 1-2% 误差）

**实验二：主对比实验（控制所有必须相同的条件）**
- 在相同数据集、划分、指标下对比我们的完整系统 vs P5
- 同时报告 P5 在 512 / 1024 / 2048 token 窗口下的性能曲线
- 预期：短序列任务上性能可比，长周期/多样性任务上明显领先

**实验三：消融实验**
- 去掉长期记忆（仅保留短期记忆）：周期性购买任务性能下降
- 去掉 RL 探索（改为确定性策略）：多样性指标下降
- 去掉不确定性自适应（改为固定 ε）：探索效率下降
- 去掉负反馈信号：reward 质量下降

**实验四：推理效率对比**
- 相同硬件、相同 batch size 下的延迟 (ms/query) 与 throughput (queries/sec)
- Beam search (B=20) vs 离散动作 RL 前向

**实验五：长期收益对比（仿真环境）**
- 构造用户模拟器（基于 P5 生成的用户响应或真实日志的 replay 环境）
- 模拟 30 天交互，对比长期留存率、多样性指数、累积奖励
- **这是展示"短期 CTR 略降但长期反超"反直觉发现的关键实验**

---

## 6. 相关工作与竞争格局

详见 `citation.md`，关键竞品总结：

| 论文 | RL | 外部记忆 | 层次化记忆 | 不确定性 | 主动探索 | 推理效率 |
|------|:--:|:------:|:--------:|:------:|:------:|:------:|
| **MR.Rec (2025.10)** | ✅ | ✅ (RAG文本) | ❌ | ❌ | ❌ | ❌ |
| **CoRAL (2024.03)** | ✅ | ✅ (CF信号) | ❌ | ❌ | ❌ | ❌ |
| **MocDT (CIKM 2024)** | ❌ (DT) | ❌ | ❌ | ❌ | 部分 | ❌ |
| **ARAG (SIGIR 2025)** | ❌ | ✅ (Blackboard) | 部分 | ❌ | ❌ | ❌ |
| **本系统** | ✅ | ✅ (向量+时间衰减) | ✅ | ✅ | ✅ | ✅ |

---

## 7. 下一步计划

1. **复现 P5 Baseline（1 周）**：在 Beauty 数据集上跑通 P5 全部 5 个任务 + 零样本评估，建立性能基准。
2. **实现记忆系统 MVP（2 周）**：ChromaDB 存储 + 短期/长期记忆 + 向量检索 + 时间衰减。
3. **实现 RL 决策层（2 周）**：POMDP 环境 + 离散动作空间 + CQL/IQL 离线训练 + 不确定性自适应探索。
4. **搭建仿真环境（3 周）**：用户模拟器对比 P5 与 RL 在长期多样性/留存指标上的差异。
5. **撰写论文（4 周）**：对标 RecSys / SIGIR 格式。
6. **投稿目标**：RecSys 2026（deadline 约 2026 年 5-6 月）或 CIKM 2026（deadline 约 2026 年 3-4 月）。
